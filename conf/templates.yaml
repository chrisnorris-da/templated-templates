module-name: RoleModel

templates:
  - base:
     baseName: AssetOwner
     signatory : operator
     operator : owner
    derive:
       name: Invitation
  - base:
     baseName: AssetOwner2
     signatory : operator2
     operator : owner2
    derive:
       name: Invitation2
